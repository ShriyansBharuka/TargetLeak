# Normalise to LF in the repository regardless of who commits.
# Authored on Windows; CI and most contributors are on Linux, and mixed
# endings turn every future diff into noise.
* text=auto eol=lf

*.png binary
*.parquet binary
